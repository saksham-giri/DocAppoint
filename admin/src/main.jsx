import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import axios from'axios';import'./style.css';
const API=import.meta.env.VITE_API_URL||'http://localhost:5000/api';
function App(){const[data,setData]=useState([]),[error,setError]=useState('');useEffect(()=>{const t=localStorage.getItem('docappoint_token');if(!t){setError('Open the patient app, sign in with an admin account, then return here.');return}axios.get(API+'/admin/appointments',{headers:{Authorization:'Bearer '+t}}).then(r=>setData(r.data)).catch(e=>setError(e.response?.data?.message||'Unable to load dashboard.'))},[]);return <main><header><div><small>DOCAPPOINT / ADMIN</small><h1>Operations overview</h1></div><button onClick={()=>location.href='/'}>Exit</button></header><section className="metrics"><div>Appointments<b>{data.length}</b></div><div>Scheduled<b>{data.filter(x=>x.status==='scheduled').length}</b></div><div>Completed<b>{data.filter(x=>x.status==='completed').length}</b></div></section><section className="panel"><h2>Latest appointments</h2>{error?<p>{error}</p>:data.map(x=><div className="row"><span><b>{x.patient?.name}</b><small>{x.patient?.email}</small></span><span>{x.doctor?.name}<small>{x.doctor?.specialty}</small></span><strong>{x.date} · {x.time}</strong><em>{x.status}</em></div>)}</section></main>}createRoot(document.getElementById('root')).render(<App/>);
